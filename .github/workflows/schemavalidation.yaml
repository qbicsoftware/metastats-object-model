name: Schema Validation
on: [push]

jobs:
  build:
    name: Validation
    runs-on: ubuntu-latest
    steps:
      - name: list workspace
        run: 'll $GITHUB_WORKSPACE'
      - name: Install SDKMAN
        run: 'bash $GITHUB_WORKSPACE/setupenv.sh'